require 'rails_helper'

RSpec.describe "pages/home.html.erb", type: :view do
  it "should return Rails default text, but can not get this to work"
  it "it returns NoMethodError: undefined method `visit' for #<RSpec::ExampleGroups::PagesHomeHtml..."
  # do
  #   visit root_url
  #   expect(page).to have_content "Pages#home"
  # end
end
